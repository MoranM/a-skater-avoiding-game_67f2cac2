class Player {\\n    constructor(context, x, y) {\\n        this.context = context;\\n        this.x = x;\\n        this.y = y;\\n        this.width = 50;\\n        this.height = 50;\\n        this.color = 'red';\\n        this.speed = 5;\\n\\n        window.addEventListener('keydown', this.handleKeyDown);\\n    }\\n\\n    handleKeyDown = (event) => {\\n        switch(event.key) {\\n            case 'ArrowLeft':\\n                this.x -= this.speed;\\n                break;\\n            case 'ArrowRight':\\n                this.x += this.speed;\\n                break;\\n            case 'ArrowUp':\\n                this.y -= this.speed;\\n                break;\\n            case 'ArrowDown':\\n                this.y += this.speed;\\n                break;\\n        }\\n    };\\n\\n    update() {\\n        // Add logic for player updates, collision, etc.\\n    }\\n\\n    render() {\\n        this.context.fillStyle = this.color;\\n        this.context.fillRect(this.x, this.y, this.width, this.height);\\n    }\\n}\\n\\nexport default Player;